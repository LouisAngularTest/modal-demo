# modal
```shell
  Demo Introduction:
  Home page: app.component has a button "Open Modal" to trigger Modal
  Modal Page: modal.component has Test title, Test body, Confirm button and Cancel button,
  Cancel button could close the modal, When the modal pops up there should be an overlay behind it
  that stops the user from clicking anything else on the page
```
